var elem = document.querySelector('.main-carousel-fr');
    var flkty = new Flickity( elem, {
        // options
        cellAlign: 'left',
        contain: true
    });

    var elem = document.querySelector('.raw-mat01');
    var flkty = new Flickity( elem, {
        // options
        cellAlign: 'left',
        contain: true
    });

    // element argument can be a selector string
    //   for an individual element
    var flkty = new Flickity( '.main-carousel-fr', {
    // options
    });